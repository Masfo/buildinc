name: C/C++ CI

on:
  push:
    branches: [ "cf-patch" ]
  pull_request:
    branches: [ "cf-patch" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: clang-format Check
      uses: jidicula/clang-format-action@v4.11.0
